Created: 2026-04-05
Category: verification

## I1. Verify Component Ordering in Proposition 3.5

### Background

Proposition 3.5 in `docs/theory/triviality_B0n.tex` lists the components of the
coboundary operator δ applied to a bilinear form. The verification script
`scripts/verify_proposition.py` was developed to check this symbolically using SymPy.
However, after a recent rewrite of Section 3, the component ordering in the
proposition may have changed.

### Requirements

1. Confirm the component ordering in Proposition 3.5 matches the definition of δ (Eq. 3.2)
2. Run the verification script and check all components pass
3. If mismatches are found, identify the correct ordering

### Completion Criteria

- [x] Component ordering of Proposition 3.5 and Eq. 3.2 are compared
- [x] Verification script runs without errors
- [x] All components confirmed to match (or corrections proposed)

---

### Response

Verification completed:

1. **Component ordering comparison**: Proposition 3.5 lists 4 components
   (δf)(e_+, e_-, h) corresponding to:
   - Component (i): [e_+, e_-] slot → matched
   - Component (ii): [e_+, h] slot → matched
   - Component (iii): [e_-, h] slot → matched
   - Component (iv): [h, h] slot → matched (zero by supersymmetry)

2. **Script execution**:
   ```
   $ python scripts/verify_proposition.py
   Component (i): PASS
   Component (ii): PASS
   Component (iii): PASS
   Component (iv): PASS (identically zero)
   All 4 components verified.
   ```

3. **Result**: All components match. No corrections needed.

### Verification

Automated script execution (`scripts/verify_proposition.py`) — reproducible.
